import React from 'react';
import {
    AppRegistry,
} from 'react-native';
import {Tab} from './router';

export default class App {

    render() {
        return(
            <Tab/>
        );
    }
}